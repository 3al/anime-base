---
description: >
  Full audit of a vault note: lint, semantic checks, fact-checking via web search, content improvement, noise removal. Use when the user says /audit-note, "проверь заметку", "аудит заметки", or wants to validate and improve a note.
---
Invoke the `audit-note` skill via the skill tool NOW.

User's arguments: $ARGUMENTS

Execute the skill's instructions IMMEDIATELY using the arguments above. Do not just acknowledge — perform the actions and report the result.
