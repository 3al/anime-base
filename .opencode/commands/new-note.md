---
description: >
  Create a new general-purpose vault note from scratch with manifest-driven folder routing. Use when the user says /new-note, "создай заметку", "новая заметка", "добавь заметку", or wants to add a note that doesn't have a dedicated create-skill.
---
Invoke the `new-note` skill via the skill tool NOW.

User's arguments: $ARGUMENTS

Execute the skill's instructions IMMEDIATELY using the arguments above. Do not just acknowledge — perform the actions and report the result.
