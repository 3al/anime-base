---
description: >
  Audit and retrofit the image-handling vision-gate into hand-written theme skills that download, pick, classify, or visually verify images. Scans .claude/skills/*/SKILL.md (non-managed) for image-work signals, flags those missing the vision-gate (banner + active multimodality check), and proposes the retrofit per-file. Also seeds/refreshes the canonical image-handling section in CLAUDE.md. Use when the user says /vision-gate-audit, "проверь скиллы на vision-gate", "ретрофит vision-gate", "дотяни image-конвенцию до скиллов", or wants hand-written image skills to obey the multimodality rule.
---
Invoke the `vision-gate-audit` skill via the skill tool NOW.

User's arguments: $ARGUMENTS

Execute the skill's instructions IMMEDIATELY using the arguments above. Do not just acknowledge — perform the actions and report the result.
