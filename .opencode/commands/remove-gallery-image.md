---
description: >
  Remove one or all images from a note's ## Галерея section. By index (row-major, left-to-right then top-to-bottom) or all at once ("all"). Detects whether image files are embedded in other notes across the vault and, if so, asks the user via multi-choice whether to delete the files everywhere, only unlink in this note, or cancel. Rebuilds the gallery table after removal; with "all" the whole ## Галерея section is removed. Use when the user says /remove-gallery-image, "удали фото из галереи", "убери картинку N", "удали всю галерею", "remove gallery image", or wants to drop specific or all images from a note's gallery section.
---
Invoke the `remove-gallery-image` skill via the skill tool NOW.

User's arguments: $ARGUMENTS

Execute the skill's instructions IMMEDIATELY using the arguments above. Do not just acknowledge — perform the actions and report the result.
