---
description: >
  Add a new note_kind (specialized note type — anime, book, recipe, podcast, etc.) to the vault. Interactive 5-phase Q&A skill: patches SYSTEM/* governance docs, registers folder in vault-manifest, and generates a thematic /new-<kind> skill. Use when the user says /add-note-kind, "добавь тип заметок", "новый kind", "поддержи тип <X>", or wants to extend the vault with a specialized note type.
---
Invoke the `add-note-kind` skill via the skill tool NOW.

User's arguments: $ARGUMENTS

Execute the skill's instructions IMMEDIATELY using the arguments above. Do not just acknowledge — perform the actions and report the result.
