---
description: >
  Add images (poster or gallery items) to an existing vault note. Copies the file(s) into attachments/, updates frontmatter (poster only), inserts embeds into the body. Renders as a gallery navbar via the Obsidian Image Toolkit plugin. Use when the user says /add-images, "добавь постер", "добавь картинки в галерею", "прикрепи изображение к карточке", or wants to attach poster/gallery files to an existing note.
---
Invoke the `add-images` skill via the skill tool NOW.

User's arguments: $ARGUMENTS

Execute the skill's instructions IMMEDIATELY using the arguments above. Do not just acknowledge — perform the actions and report the result.
