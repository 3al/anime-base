---
description: >
  Mark a vault note as reviewed. Use when the user says /verify, "пометь как проверенную", "approve note", or wants to set quality/co_authored/updated on a note.
---
Invoke the `verify` skill via the skill tool NOW.

User's arguments: $ARGUMENTS

Execute the skill's instructions IMMEDIATELY using the arguments above. Do not just acknowledge — perform the actions and report the result.
