---
description: >
  Rename or move a vault note, updating all WikiLinks across the vault. Use when the user says /rename-note, "переименуй заметку", "перенеси заметку", "move note", "rename note", or wants to change a note's name or location.
---
Invoke the `rename-note` skill via the skill tool NOW.

User's arguments: $ARGUMENTS

Execute the skill's instructions IMMEDIATELY using the arguments above. Do not just acknowledge — perform the actions and report the result.
