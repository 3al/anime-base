---
description: >
  Expand a stub note into a full note. Use when the user says /expand-stub, "раскрой заглушку", "дописать заметку", "expand note", or wants to fill in a thin/empty note in the vault.
---
Invoke the `expand-stub` skill via the skill tool NOW.

User's arguments: $ARGUMENTS

Execute the skill's instructions IMMEDIATELY using the arguments above. Do not just acknowledge — perform the actions and report the result.
