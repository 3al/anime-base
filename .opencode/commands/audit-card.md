---
description: >
  Full two-axis audit of a vault card in one invocation: orchestrates /audit-by-creator (structural axis) and /audit-note (content axis) so BOTH axes land a clean (pristine=true) quality-ledger measurement on the same card, then applies fixes on both axes and leaves the card closed. Thin orchestrator — all auditing is delegated to the two managed skills (inherits their rules/improvements). Use when the user says /audit-card, "полный аудит карточки", "аудит по обеим осям", "прогони обе оси", "закрой карточку аудитом", or wants one card fully audited structurally + content-wise with both clean ledger data points.
---
Invoke the `audit-card` skill via the skill tool NOW.

User's arguments: $ARGUMENTS

Execute the skill's instructions IMMEDIATELY using the arguments above. Do not just acknowledge — perform the actions and report the result.
